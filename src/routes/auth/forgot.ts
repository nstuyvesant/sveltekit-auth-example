// Implement as a post() to avoid pre-render issues
// Lookup user_id from email
// Create JWT with user_id for 24 hours
// Send email via SendInBlue with URL
// http://localhost:3000/auth/reset/[JWT]
